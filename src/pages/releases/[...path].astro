---
// 这个文件处理 /releases/... 路径的重定向
export function getStaticPaths() {
  // 由于我们只是想重定向，可以返回一个空数组
  // GitHub Pages 不支持服务器端重定向，但我们可以创建一个客户端重定向页面
  return [];
}

// 注意：由于返回空数组，此页面实际上不会被生成
// 您需要修改 astro.config.mjs 的 redirects 配置
---

<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="refresh" content="0;url=https://crowdie.stanford.edu/">
  <title>Redirecting...</title>
</head>
<body>
  <p>如果您没有被自动重定向，<a href="https://crowdie.stanford.edu/">点击这里</a>。</p>
  <script>
    // 获取当前路径
    const path = window.location.pathname.replace('/releases/', '');
    // 重定向到目标网站
    window.location.href = `https://crowdie.stanford.edu/${path}`;
  </script>
</body>
</html> 